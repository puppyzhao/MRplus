% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_result_matrix_across_datasets.R
\name{mr_result_matrix_across_datasets}
\alias{mr_result_matrix_across_datasets}
\title{Visualise robustness of MR results across multiple datasets}
\usage{
mr_result_matrix_across_datasets(exposure_ids, outcome_ids, p_values)
}
\arguments{
\item{exposure_ids}{Character vector. IDs of exposure GWAS datasets
(e.g., \code{c("ieu-b-4872", "bbj-a-80")}).}

\item{outcome_ids}{Character vector. IDs of outcome GWAS datasets
(e.g., \code{c("ukb-b-16890", "ebi-a-GCST90013972")}).}

\item{p_values}{Numeric vector. P-value thresholds for instrument
selection (e.g., \code{c(1e-5, 5e-6, 5e-7, 5e-8)}).}
}
\value{
A ggplot object showing exposure–outcome pairs as a bubble plot:
\itemize{
\item Bubble size = count of significant MR results (p < 0.05).
\item Bubble color = \code{-log10(mean p-value)}.
The function also returns a data frame of aggregated results invisibly.
}
}
\description{
This function runs Mendelian randomization (MR) analyses for multiple
exposure–outcome pairs across several instrument selection thresholds.
It aggregates significance counts and mean p-values, and visualises the
results in a bubble plot.
}
\details{
\itemize{
\item Three MR methods are applied: IVW, MR-Egger, and Weighted Median.
\item For each exposure–outcome pair and set of thresholds, the function
calculates:
\itemize{
\item the number of significant MR results (\code{p < 0.05}),
\item the mean p-value,
\item and its \code{-log10} transformation.
}
\item Bubble sizes are capped at 12 (the maximum number of possible tests).
}
}
\examples{
\dontrun{
mr_result_matrix_across_datasets(
  exposure_ids = c("ieu-b-4872", "bbj-a-80"),
  outcome_ids = c("ukb-b-16890", "ebi-a-GCST90013972"),
  p_values = c(1e-5, 5e-6, 5e-7, 5e-8)
)
}

}
\seealso{
\code{\link[TwoSampleMR]{extract_instruments}},
\code{\link[TwoSampleMR]{extract_outcome_data}},
\code{\link[TwoSampleMR]{harmonise_data}},
\code{\link[TwoSampleMR]{mr}}
}
