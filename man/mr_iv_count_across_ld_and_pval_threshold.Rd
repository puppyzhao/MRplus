% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_iv_count_across_ld_and_pval_threshold.R
\name{mr_iv_count_across_ld_and_pval_threshold}
\alias{mr_iv_count_across_ld_and_pval_threshold}
\title{Visualize the number of instruments across LD pruning and p-value thresholds}
\usage{
mr_iv_count_across_ld_and_pval_threshold(
  exposure_id,
  p_values = c(5e-08, 5e-07, 5e-06, 1e-05),
  r2_kb_combinations = list(c(1e-04, 1000), c(0.001, 10000), c(0.01, 1e+05))
)
}
\arguments{
\item{exposure_id}{Character. The ID of the exposure GWAS (e.g., "ieu-b-4872").}

\item{p_values}{Numeric vector. A set of p-value thresholds to test for instrument selection.
Default is \code{c(5e-8, 5e-7, 5e-6, 1e-5)}.}

\item{r2_kb_combinations}{List of numeric vectors. Each element should be of the form
\code{c(r2, kb)} specifying the LD pruning threshold (r2) and window size (kb).
Default is \code{list(c(0.0001, 1000), c(0.001, 10000), c(0.01, 100000))}.}
}
\value{
A ggplot object showing SNP counts under different thresholds and LD settings.
The function also prints progress information to the console.
}
\description{
This function extracts instruments for a given exposure from MR-Base
(via \code{TwoSampleMR::extract_instruments}) under multiple
p-value thresholds and LD pruning settings. It summarizes the SNP counts
and generates a visualization of how instrument selection is influenced
by these parameters.
}
\details{
\itemize{
\item The "Default" setting corresponds to \code{r2 = 0.001, kb = 10000}, which is
the default in \code{TwoSampleMR}.
\item The output plot shows:
\itemize{
\item Line + points: SNP counts under the default LD pruning
\item Triangles and squares: SNP counts under min and max LD pruning
\item Red error bars: range (min–max) across LD pruning at each p-value threshold
\item Labels: annotated min and max values
}
}
}
\examples{
\dontrun{
mr_iv_count_across_ld_and_pval_threshold("ieu-b-4872")
}

}
\seealso{
\code{\link[TwoSampleMR]{extract_instruments}}
}
