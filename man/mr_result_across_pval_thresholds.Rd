% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_result_across_pval_thresholds.R
\name{mr_result_across_pval_thresholds}
\alias{mr_result_across_pval_thresholds}
\title{Compare MR results across different IV selection thresholds}
\usage{
mr_result_across_pval_thresholds(exposure_id, outcome_id)
}
\arguments{
\item{exposure_id}{Character. The ID of the exposure GWAS
(e.g., "ieu-b-4872").}

\item{outcome_id}{Character. The ID of the outcome GWAS
(e.g., "ukb-b-16890").}
}
\value{
A ggplot object showing MR p-values across different IV selection
thresholds and methods.
The function also prints the underlying results table to the console.
}
\description{
This function performs Mendelian randomization (MR) analyses for a given
exposure–outcome pair across multiple instrument selection thresholds.
It extracts instruments at each p-value threshold, harmonises exposure–
outcome data, runs MR with different methods (IVW, MR-Egger, and Weighted
Median), and visualises the resulting p-values.
}
\details{
\itemize{
\item Tested thresholds are fixed at \code{c(1e-5, 5e-6, 5e-7, 5e-8)}.
\item Three MR methods are included:
\itemize{
\item IVW (\code{mr_ivw})
\item MR-Egger regression (\code{mr_egger_regression})
\item Weighted Median (\code{mr_weighted_median})
}
\item P-values are plotted on a \code{log10} scale (minimum set to 1e-4).
\item Combinations that fail are imputed with \code{p = 1}.
}
}
\examples{
\dontrun{
mr_result_across_pval_thresholds("ieu-b-4872", "ukb-b-16890")
}

}
\seealso{
\code{\link[TwoSampleMR]{extract_instruments}},
\code{\link[TwoSampleMR]{extract_outcome_data}},
\code{\link[TwoSampleMR]{harmonise_data}},
\code{\link[TwoSampleMR]{mr}}
}
